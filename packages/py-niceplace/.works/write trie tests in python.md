Consider the tests at #packages/ts-trie/test .

Using PyTest, reproduce them for the python trie defined at #packages/py-niceplace/trie.py

Place the tests in:

```
packages/py-nice@place/test/trie/*
```